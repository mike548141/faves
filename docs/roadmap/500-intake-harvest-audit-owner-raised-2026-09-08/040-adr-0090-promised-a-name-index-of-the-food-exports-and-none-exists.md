- [x] 🔎 **ADR 0090 promised a name index of the prior food databases and none
      was ever written; the exports also hold data with no store**
      `[M][data][docs]` — found 2026-09-08 by the intake audit (session
      faves-o1), which read all six files.

  ✅ **DELIVERED 2026-09-08 (session faves-o1) — OPTION 1**, as the owner
  authorised. `data/intake/name-index.json`, generated by
  `tools/intake_index.py`, documented in `data/README.md` and
  `docs/ARCHITECTURE.md`, and recorded in an **Addendum** to ADR 0090 (its
  *Decision* untouched — `docs/decisions/README.md` forbids editing an accepted
  record's substance, and the same file shows Addendum as the house's own
  practice on 0017 and 0060).

  🔎 **What it holds**, verbatim from the tool:

  ```
  wrote data/intake/name-index.json — 152 name(s) from 5 export(s);
  63 have a data/products/ record, 89 have none.
  ```

  Per row: name, brand where the export gives one, which export file(s) named
  it, and the matching `data/products/` id or `null`. **89 names with no
  record** — the five-SKU Fresh 'n Fruity yoghurt line among them, exactly as
  this item said.

  🚩 **That 89 is NOT the item's "about thirty", and the item is not wrong.**
  The ~30 counted *branded packaged items*; 152 is every name the exports
  carry, and most of the unmatched remainder is homemade dishes, restaurant
  dishes and generic foods that were never candidates for a packaged-product
  record. Both numbers are true of different populations. The index does not
  classify rows, because classifying them is inference and this file is a list
  of what was collected.

  🛑 **NO NUTRITION FIGURE CAME THROUGH — not one.** ADR 0090 measured those
  exports wrong where it mattered (sweetcorn 62% against a label reading 48%,
  two ingredients dropped), so copying their numbers in would launder bad data
  into the repo behind its own provenance rules. Names and source file only.
  `conversation_export_partial.json` was **never opened**: it is eating events,
  barred by ADR 0090 rule 2 and by CLAUDE.md's absolute no-health-data rule. It
  is named in the index's header solely to record that it was seen and refused,
  because "nobody looked" and "looked and refused" are otherwise identical.

  🔑 **Four rows carried something personal and are recorded with it removed**,
  each flagged `redacted` in the file: two Ruth attributions, one Clements
  family attribution, and one order modification that reads as a personal
  dietary note. The redaction table is explicit and reviewable rather than a
  regex — a rule keyed on apostrophe-s would eat Wattie's, Griffin's, Pam's,
  Rose's, Danny's, Lisa's and Grandpa's, which are brands. **Also seen and left
  behind entirely:** the Gemini export's `safety_flags` ("ASTHMA WARNING",
  "ALLERGY RISK", "SUGAR TRAP") and `mike_food_database.json`'s household notes
  — none of which the index reads.

  ⚖️ **The `product` column is a token-match HINT and says so in the file.** A
  1 kg tub and a 500 g tub share every token. 20 pairs the heuristic proposed
  were reviewed and refused by hand (`REVIEWED_WRONG` in the tool, each with a
  reason) — "Wattie's Spaghetti in Tomato Sauce" is not `watties-tomato-sauce`,
  and a composite meal that mentions raspberry jam is not the jam. Its value is
  the negative direction: a name with no record is a thing collected once and
  recorded nowhere else here.

  🛑 **`intake/**` is gitignored, so the tool degrades rather than fails**: with
  no `intake/` it prints `intake not present` and exits 0, leaving the committed
  index alone. `--check` regenerates and diffs when the material IS present.

  ❌ **Options 2 and 3 not taken.** The owner chose 1. The restaurant-dish
  nutrition estimates this item raises still have **no store and no screen**
  (ADR 0047); the index names those dishes and holds none of their figures, so
  the decision this item asks for is untouched and still open.

  **What is in `intake/ingredients/`** — five nutrition exports the owner
  collected before Faves, plus one chat export:

  | File | Rows | What it carries |
  |---|---:|---|
  | `all_foods_with_provenance.json` | 107 | name, category, serving size, per-serving macros, provenance, capture date |
  | `full_food_database (1).json` | 64 | the same, without provenance |
  | `mike_food_database.json` | 17 | brand, per-100 g **and** per-serving, source notes |
  | `food_database (1).json` | 9 | per-serving and per-100 g |
  | `Gemini food DB 1st 20 photos` | 19 | the richest — taxonomy, **ingredient lists**, **allergens (contains / may contain)**, macros, provenance |
  | `conversation_export_partial.json` | 9 | chat messages logging meals eaten. **Correctly excluded** — ADR 0090 rule 2 bars eating events |

  🛑 **The unkept promise.** ADR 0090 ruled the exports *"are a good **index of
  what exists**; they are not data"* and *"kept as a name index only."* **No
  such index exists in the repo.** The exports were read once, judged, and left
  in `intake/`. Verified: every brand in them that has no product record —
  Vogel's, Freya's, Fresh 'n Fruity, Nescafé, Best Foods, Big Ben, Lisa's,
  Danny's — returns **0 hits** in both `site/` and `data/`. About thirty
  branded items are named there and nowhere else, including a whole yoghurt
  line (five SKUs with full nutrition) that was never photographed.
  🔑 An accepted record describing a thing that does not exist is worse than a
  gap, because the next reader stops looking.

  🎯 **And they hold something with no home at all.** The exports carry
  **estimated nutrition for restaurant dishes at venues that are live in the
  app** — Nasi Goreng at KK Malaysian, Chicken Biryani at Spices, Combination
  Fried Rice at Takeaway @ Churton, a Pizza Hut Hawaiian. ADR 0047 governs:
  name the screen that renders it. There is none, so the payload is out.
  `data/products/` is a **packaged-product** store, so that is out too. This is
  a decision, not a tidy-up: either a store exists for third-party nutrition
  estimates about a venue's dish, or the data stays in `intake/` and the ADR
  should say so rather than promising an index.

  📋 **Options.** (1) Write the index ADR 0090 promised — a flat list of names
  with their source file, no nutrition, purely so a future session can see what
  was collected. Cheapest, and it keeps the record honest. (2) Ingest the
  packaged-product rows properly into `data/products/` where a photograph
  exists to back them, and index the rest. (3) Amend ADR 0090 to say the
  exports are kept in `intake/` and referenced nowhere, which is the truth
  today. Recommendation: **(1)**, because it costs almost nothing and it is
  what the accepted record already says was done.
