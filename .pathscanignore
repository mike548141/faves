# Paths exempt from pathscan (gitignore-style globs, one per line).
# Keep this list SHORT and reasoned — every entry is a hole in the boundary.

# The board index is GENERATED, and its link TEXT is a path-shaped string:
# `[040-theme-2-location-maps/README.md](roadmap/040-theme-2-location-maps/…)`.
# pathscan reads the text half as a bare path, resolves it from docs/, and finds
# nothing — 49 findings, one per section, every commit, none of them real. The
# link itself resolves; linkscan (enforced) is what actually checks that, and it
# passes. A warn-only check that fires 49 times on every commit is a check
# nobody reads, which is the failure mode this repo has already recorded twice.
docs/ROADMAP.md
