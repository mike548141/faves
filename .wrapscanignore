# Paths exempt from wrapscan (gitignore-style globs, one per line).
# Keep this list SHORT and reasoned — every entry is a hole in the boundary.

# The board index is GENERATED (tools/board.py; the `board` floor check keeps it
# in step with docs/roadmap/). Its long lines are the generator's: the
# do-not-hand-edit banner is 115 columns and an index line carries a title plus
# a path. Rewrapping it by hand is not a fix — the next `board rebuild`
# overwrites the wrap — and wrapping it in the generator is atelier's call, not
# a local edit to a tool this repo does not own. Source prose is NOT exempt:
# every hand-written line under docs/roadmap/ is still scanned.
docs/ROADMAP.md
